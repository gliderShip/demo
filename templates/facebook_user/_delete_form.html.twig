<form method="post" action="{{ path('facebook_user_delete', {'id': facebook_user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ facebook_user.id) }}">
    <button class="btn">Delete</button>
</form>
